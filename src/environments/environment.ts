// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyDYu_dMCWofq5TiqWkt9aRe2CTwCalq-_w",
    authDomain: "seeinghome-353d3.firebaseapp.com",
    databaseURL: "https://seeinghome-353d3.firebaseio.com",
    projectId: "seeinghome-353d3",
    storageBucket: "seeinghome-353d3.appspot.com",
    messagingSenderId: "858050242189",
    appId: "1:858050242189:web:061b5a4431d40f0c"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
